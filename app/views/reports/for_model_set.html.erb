<% title ("Report for " + @start_date.strftime("%b %d, %Y") + " to " + @end_date.strftime("%b %d, %Y")) %>
<ul class="breadcrumb">
    <li>
    <%= link_to "Reports", reports_path %>
    <span class="divider">/</span>
    </li>
    <li class="active">Equipment Model Set</li>
</ul>
<%= render :partial => 'reports/report_generator'%>

<h2>Set of Equipment Models</h2><hr />

<%= render :partial => "data_tables" , :object => @data_tables%>