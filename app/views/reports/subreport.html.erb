<% title ("#{@object.class} Report for " + @start_date.strftime("%b %d, %Y") + " to " + @end_date.strftime("%b %d, %Y")) %>

<ul class="breadcrumb">
  <li>
    <%= link_to "Reports", reports_path %>
    <span class="divider">/</span>
  </li>
  <li class="active">
    <%= @object.name.singularize %>
  </li>
</ul>

<%= render :partial => 'reports/report_dates'%>
<h2 class = "equipment_model">
  <%= "#{@object.class}: #{@object.name.singularize}" %>
</h2>
<%= render :partial => "data_tables" , :object => @data_tables%>
